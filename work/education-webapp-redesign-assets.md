# Minimum Permission Lab Asset Audit

## 조사 범위

조사일: 2026-08-29

재확인일: 2026-08-30 (코드-led 마감 리디자인)

다음 경로에서 이미지 import, `<img>`, `srcset`, preload, CSS `url()`·배경 이미지를 검색했습니다.

- `public/`
- `src/`
- `index.html`
- `src/styles/`

학습자 화면에서 확인된 이미지는 없으며 `public/favicon.svg`만 존재합니다.

## 판정표

| 원본 | 화면·역할 | 판정 | 새 파일 | 접근성 | 상태 | 롤백 |
|---|---|---|---|---|---|---|
| `public/favicon.svg` | 브라우저 탭의 서비스 식별용 브랜드 자산 | 자동 교체 금지. 로고·마크는 출처와 정체성이 중요합니다. | 없음 | 페이지 본문 정보가 아니므로 favicon으로만 유지 | 확인 완료, 참조 변경 없음 | `index.html`의 기존 favicon 참조를 유지 |
| 없음 | 시작·명세·심사·보고서의 학습 화면 | 생성 이미지가 학습 사실이나 실제 앱 화면으로 오인될 수 있어 자동 생성하지 않음 | 없음 | 이미지 대체 텍스트 변경 없음 | `imagegen` not run | 이미지 참조를 추가하지 않음 |

## 안전 결론

현재 UI에는 교체할 일반 장식·개념 일러스트가 없습니다. 생성 이미지·스크린샷·지도·차트·로고를 새로 삽입하지 않았고, `imagegen`은 호출하지 않았습니다. 향후 자산이 추가되면 대상 학년·학습 역할·비율을 먼저 검토하고, 사실·정체성·증거 자산은 `human review required`로 남깁니다.

## 2026-08-30 재확인

이번 헤더·학습 개요·상호작용 스타일 보완에도 이미지 import, `<img>`, `srcset`, CSS `url()`·외부 폰트는 추가되지 않았습니다. `/private/tmp`의 모바일 Chromium 검증 설정은 테스트 후 삭제했으며 저장소 자산이 아닙니다. `imagegen`은 계속 `not run`입니다.
